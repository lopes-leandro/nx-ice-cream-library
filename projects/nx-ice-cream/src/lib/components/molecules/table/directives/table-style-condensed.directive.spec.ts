import { TableStyleCondensedDirective } from './table-style-condensed.directive';

describe('TableStyleCondensedDirective', () => {
  it('should create an instance', () => {
    const directive = new TableStyleCondensedDirective();
    expect(directive).toBeTruthy();
  });
});
