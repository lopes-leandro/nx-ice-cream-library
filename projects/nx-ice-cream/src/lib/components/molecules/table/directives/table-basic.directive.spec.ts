import { TableBasicDirective } from './table-basic.directive';

describe('TableBasicDirective', () => {
  it('should create an instance', () => {
    const directive = new TableBasicDirective();
    expect(directive).toBeTruthy();
  });
});
